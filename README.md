# faker-based python application
This code generate a random fact
In order to run use following command:

```
make d-homework-i-run
```
In order to stop the container use following command:

```
make d-homework-i-purge
```